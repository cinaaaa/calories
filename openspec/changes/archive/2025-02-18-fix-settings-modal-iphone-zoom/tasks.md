## 1. Settings modal input zoom fix

- [x] 1.1 Ensure number inputs in Settings modal have minimum font-size 16px so iOS does not auto-zoom on focus (e.g. add scoped style for `.field-input` in `SettingsModal.svelte`)
